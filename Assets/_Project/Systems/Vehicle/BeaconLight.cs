// BeaconLight system removed (B 키 비콘 메카닉 제거됨).
// 이 파일은 Unity Project 창에서 직접 삭제해도 됩니다 (BeaconLight.cs + .meta).
//
// 만약 prefab/씬에 BeaconLight 컴포넌트 인스턴스가 남아있으면 Missing Script 경고가 뜨므로,
// 인스펙터에서 제거하거나 prefab 편집 모드에서 삭제 후 이 파일 삭제 권장.

namespace LastPatrol.Systems.Vehicle
{
    // intentionally empty — placeholder for clean removal
}
